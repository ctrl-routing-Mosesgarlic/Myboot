"""pyboot — MyBoot's build/test/install tooling.

A small, decentralized CLI (one responsibility per module) that replaces the
project's shell scripts. See ``cli`` for the command surface, or run
``python manage.py --help``.
"""
__version__ = "1.0.0"
