# Rocq (formerly Coq) — deductive algorithm proofs (stretch goal, report §6.6)

Goal (long horizon): machine-checked deductive proofs of selected algorithm-level
properties — e.g. that the policy engine's decision function is total and its
recommendation is always explainable, or refinement between the TLA+ model and a
Rust-level model. This is the seL4/CompCert-scale ambition, explicitly future work.

Placeholder: `BootPolicy.v`. Toolchain (The Rocq Prover) to be added if activated.
